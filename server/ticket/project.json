{
  "name": "server-ticket",
  "root": "server/ticket",
  "projectType": "library",
  "tags": ["backend", "go"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go build ./...",
        "cwd": "server/ticket"
      }
    },
    "test": {
      "executor": "@nx-go/nx-go:test"
    },
    "tidy": {
      "executor": "@nx-go/nx-go:tidy"
    }
  }
}
