# https://moonrepo.dev/docs/config/project
$schema: '../../.moon/cache/schemas/project.json'

id: 'be-web'
language: 'go'
type: 'library'

project:
  description: 'Web server handlers and middleware'
  channel: '#backend'
  
