{
  "name": "cortex-tests",
  "private": true,
  "scripts": {
    "test": "playwright test --reporter=list",
    "postinstall": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}
