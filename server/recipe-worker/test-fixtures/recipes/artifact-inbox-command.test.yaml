tests:
  - name: inbox_roundtrip
    description: Command op reads artifact from inbox
    inputs: {}
    want:
      read_content: "hello inbox"
    wantArtifacts:
      - payload.txt
    wantErr: false
