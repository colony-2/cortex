id: simple_echo
desc: Very simple echo recipe for testing
version: '1.0'
sequence:
- id: echo_cmd
  op: command_execution
  inputs:
    run: '{{ "echo ''" + (has(inputs.text) ? inputs.text : "<no value>") + "''" }}'
inputs:
  text: hello
outputs:
  result: '{{ sequence.echo_cmd.outputs.stdout }}'
