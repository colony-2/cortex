tests:
- name: basic_command_execution
  description: Test basic command execution
  inputs: {}
  want:
    stdout: "Testing command execution"
    success: true
    exit_code: 0
    stderr: ""
    error_message: ""
    timed_out: false
  wantErr: false
# Note: The test command_execution activity doesn't simulate errors
# so this test case isn't meaningful without a real command executor
- name: command_with_error
  description: Test command that returns error (simplified for test activity)
  inputs: {}
  want:
    stdout: "Testing command execution"
    success: true
    exit_code: 0
    stderr: ""
    error_message: ""
    timed_out: false
  wantErr: false
