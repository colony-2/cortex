# Minimal recipe example - demonstrates basic unified structure
name: hello_world
description: A minimal recipe that demonstrates basic structure
version: "1.0"

# Root is a single operation
op: echo_activity
inputs:
  message: "Hello, {{ .inputs.name }}!"
  name: "{{ .inputs.name }}"