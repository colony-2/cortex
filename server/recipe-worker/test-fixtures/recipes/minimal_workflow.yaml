id: hello_world
desc: A minimal recipe that demonstrates basic structure
version: '1.0'
op: echo_activity
inputs:
  message: '{{ "Hello, " + (has(inputs.name) ? inputs.name : "<no value>") + "!" }}'
  name: '{{ has(inputs.name) ? inputs.name : "<no value>" }}'
