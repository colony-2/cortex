# Minimal recipe example - demonstrates basic unified structure
id: hello_world
desc: A minimal recipe that demonstrates basic structure
version: "1.0"

# Root is a single operation
op: echo_activity
inputs:
  message: "Hello, {{ .inputs.name }}!"
  name: "{{ .inputs.name }}"