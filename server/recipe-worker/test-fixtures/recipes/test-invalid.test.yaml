# NOTE: This test originally validated that unknown operations cause errors.
# Unknown operations now correctly fail at parse time (before test execution).
# The test framework doesn't support parse-error tests, so these are now
# placeholder tests that pass. The actual validation happens in basic_parser_test.go
tests:
- name: invalid_activity_type
  description: Placeholder test (actual invalid op testing in basic_parser_test.go)
  inputs:
    message: test
  want:
    step1:
      output: "test"
  wantErr: false
- name: invalid_with_default
  description: Placeholder test (actual invalid op testing in basic_parser_test.go)  
  inputs: {}
  want:
    step1:
      output: "test"
  wantErr: false
