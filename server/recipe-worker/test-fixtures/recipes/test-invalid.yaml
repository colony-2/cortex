name: test-invalid
description: Test recipe with invalid structure
version: "1.0"

inputs:
  - name: message
    type: string
    required: true

steps:
  - id: step1
    uses: unknown_activity_type
    inputs:
      data: "test"
  
  - id: step2
    # Missing 'uses' field
    inputs:
      data: "test"
      
  - # Missing 'id' field
    uses: command_execution
    inputs:
      run: "echo test"