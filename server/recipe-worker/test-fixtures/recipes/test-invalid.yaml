name: test-invalid
description: Test recipe with invalid structure
version: "1.0"

inputs:
  - name: message
    type: string
    required: true

sequence:
  - id: step1
    op: unknown_activity_type
    inputs:
      data: "test"
  
  - id: step2
    # Missing 'uses' field
    inputs:
      data: "test"
      
  - # Missing 'id' field
    op: command_execution
    inputs:
      run: "echo test"