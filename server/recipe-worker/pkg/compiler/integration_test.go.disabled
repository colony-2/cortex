package statemachine

import (
	"testing"
)

// Integration tests are skipped as the state machine is now part of the compiler
// and will be tested as part of the full workflow integration tests at a higher level

func TestComplexDocumentProcessingIntegration(t *testing.T) {
	t.Skip("State machine is now integrated into compiler - tested at workflow level")
}

func TestWithTemporalDevServer(t *testing.T) {
	t.Skip("State machine is now integrated into compiler - tested at workflow level")
}

func TestErrorHandlingIntegration(t *testing.T) {
	t.Skip("State machine is now integrated into compiler - tested at workflow level")
}