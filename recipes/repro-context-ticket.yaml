id: repro-context-ticket
sequence:
  - id: echo
    op: command_execution
    inputs:
      run: |
        set -euo pipefail
        echo "title={{ context.ticket.title }}" > "{{ context.environment.outbox }}/title.txt"
outputs:
  title: "{{ context.ticket.title }}"
