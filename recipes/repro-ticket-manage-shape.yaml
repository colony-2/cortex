id: repro-ticket-manage-shape
sequence:
  - id: update
    op: ticket.manage
    inputs:
      actions:
        - type: update_ticket
          ticket_id: "{{ context.actor.ticket_id }}"
          stage: open
          cell: "ui-app"
outputs:
  raw: '{{ string(sequence.update.outputs) }}'
