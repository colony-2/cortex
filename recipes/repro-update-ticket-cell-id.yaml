id: repro-update-ticket-cell-id
sequence:
  - id: update
    op: ticket.manage
    inputs:
      actions:
        - type: update_ticket
          ticket_id: "{{ context.actor.ticket_id }}"
          stage: open
          cell_id: "397t9U2SZcCqSRAKgA4JzF8Lluw"
outputs:
  raw: '{{ string(sequence.update.outputs) }}'
