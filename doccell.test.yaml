tests:
  - name: "document_go_cell"
    inputs:
      directory: "server/ops/pkg/llm"
    want:
      response:
        type: string
    wantErr: false

  - name: "document_cortex_cell"
    inputs:
      directory: "server/cortex"
    want:
      response:
        type: string
    wantErr: false

  - name: "invalid_directory"
    inputs:
      directory: "/nonexistent/path"
    wantErr: true
    wantErrContains: "no such file or directory"