id: browser-input
version: "1.0"
input_schema: {}
sequence:
  - id: approval
    op: input
    inputs:
      form:
        question: What should the recipe publish?
        type: short_answer
outputs:
  answer: "${{ sequence.approval.outputs.response }}"
