{
  "name": "container",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "server-workspace/container",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "server-workspace/container",
        "command": "go build ./..."
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "server-workspace/container",
        "command": "go test -v ./..."
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "server-workspace/container",
        "command": "go vet ./..."
      }
    }
  },
  "tags": ["go", "backend", "library"]
}