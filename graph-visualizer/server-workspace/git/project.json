{
  "name": "git",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "server-workspace/git",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "server-workspace/git",
        "command": "go build ./..."
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "server-workspace/git",
        "command": "go test -v ./..."
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "server-workspace/git",
        "command": "go vet ./..."
      }
    }
  },
  "tags": ["go", "backend", "library"]
}